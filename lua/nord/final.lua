local c = require("nord.colors").palette

return {
  VertSplit = { fg = c.lines },      -- the column separating vertically split windows
  WinSeparator = { fg = c.lines },   -- Separators between window splits.
}
